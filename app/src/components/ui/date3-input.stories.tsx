import { CalendarDateRangePicker } from "./date3-input";

const meta = {
  component: CalendarDateRangePicker,
};

export default meta;

export const date3Input = {
  render: () => <CalendarDateRangePicker />,
};

// export const Error = {
//   render: () => <CalendarDatePicker error="Error" />,
// };
